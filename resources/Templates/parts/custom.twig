{% extends 'base.twig' %}

{% block content %}
<div class="container m-10">
        <div class="row">
            <p>Hello {{name}},</p>
            {{ simple_message ?? ''}}
        </div>
        <div class="row mt-5">
            {{ message ?? ''}}
        </div>
    </div>
{% endblock %}